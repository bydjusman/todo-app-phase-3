# Database Configuration
# Use a plain SQLAlchemy connection URL (NO `psql` command, NO quotes).
# Example (Neon):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
DATABASE_URL=sqlite:///./dev.db

# Server Configuration
HOST=0.0.0.0
PORT=8000

# JWT Configuration - Generate a strong secret key for production
SECRET_KEY=generate-a-strong-secret-key-for-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Google AI Configuration
GOOGLE_API_KEY=your_google_api_key_here

# Dev convenience (Windows): enable hot reload only when needed
RELOAD=0

# Example of a generated secret key (use a tool to generate a strong random key):
# SECRET_KEY=$(openssl rand -hex 32)